# C 有一系列专门处理字符的函数
这些函数接受一个字符作为参数，如果该字符属于某特殊的类别，就返回一个非零值（true）；否则，返回0（false）
ctype.h


```c
isalpha()//判断这是不是一个字母
isdigit()//数字
islower()//小写字母
ispunct()//标点符号
tolower()//如果参数是大写字符，该函数返回小写字符；否则，返回原始参数
toupper()//vice versa
```