# ternary operator

格式：(condition) ? value_if_true : value_if_false;


```c
int x=5;
int y=6;
int max = (x>y) ? x : y;
printf("%d",max);
```

```c
bool isOnline = true;
printf("%s", (isOnline) ? "Online" : "Offline");
```

